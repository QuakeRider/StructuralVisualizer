{
  "name": "structural-visualizer",
  "version": "0.7.0",
  "private": true,
  "type": "module",
  "description": "A modular interactive learning environment for university structural geology.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "release:build": "npm run build && node scripts/package-release.mjs",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "three": "^0.180.0"
  },
  "devDependencies": {
    "vite": "^7.1.7",
    "vite-plugin-singlefile": "^2.3.0",
    "vitest": "^5.0.1"
  }
}
